# GNSS Signal Reflection Modelling using LiDAR Dense Point Clouds

## 0. 当前仓库可见内容与现状
- 当前 `gnss_ws` 仓库里可见的核心数据文件是：`2026-4-15_174245_serial-COM5(1).ubx`（原始 GNSS 接收机日志）。
- 这意味着你前期与 Claude 的大量“对话/方案文本”目前不在该仓库可见范围（至少不在当前分支的文件树里），但你的观测原始数据已经在。

## 1. 研究目标（建议明确成可验证任务）
围绕“GNSS signal reflection modelling using LiDAR dense point clouds”，拆成 4 个可交付目标：

1. **场景重建**：从 LiDAR 点云恢复可反射表面（建筑立面、玻璃、地面、围栏等）。
2. **几何可达性**：判定每个卫星-接收机时刻是否 LOS / NLOS / 潜在多径。
3. **反射路径建模**：计算一次反射路径（specular first-bounce）并估计额外路径长度、到达角、反射强度。
4. **观测解释与校正**：把模型输出映射到 GNSS 观测残差/质量指标（C/N0、伪距残差、周跳概率）。

## 2. 建议的数据与坐标统一流程（IPNL 香港数据集为核心）

### 2.1 数据清单
- IPNL 香港 LiDAR 点云（高密度、带坐标系信息）。
- 你的 UBX 原始日志（本仓库已有）。
- 星历/钟差（广播星历或精密星历）。
- 可选：街景语义标签或建筑轮廓（用于材质先验）。

### 2.2 坐标系统一（最关键）
- 统一到同一 ENU 局部坐标系（origin 设在实验区中心或基准站）。
- LiDAR：`global -> ENU`。
- GNSS 轨迹/观测几何：`ECEF -> ENU`。
- 卫星方向向量：在每个历元转到 ENU。

> 任何误差预算前，先做坐标对齐 QA：随机抽 20 个地标点检查误差统计。

## 3. 从点云到“可反射面”的工程流程

### 3.1 点云预处理
- 去噪（统计滤波 + 半径滤波）。
- 下采样（体素网格，保留几何边缘）。
- 地面分离（RANSAC 平面 + 高程阈值）。

### 3.2 几何分割与表面重建
- 平面分割：区域生长 + 法向一致性。
- 立面/地面/斜面分类：按法向与高度规则。
- 网格化：Poisson / Ball Pivoting 或直接平面片段集合。

### 3.3 反射属性赋值（先简化，后精化）
- 初版：按类别给固定反射系数（玻璃>金属>混凝土>植被）。
- 进阶：入射角相关系数、极化相关、粗糙度衰减。

## 4. GNSS 反射几何建模主线

### 4.1 LOS/NLOS 快速判定
- 接收机点到卫星方向做射线投射（ray casting）。
- 若射线与网格相交：LOS blocked -> NLOS 候选。
- 若不相交：LOS 候选，但仍可能有多径。

### 4.2 镜像法 + 射线法（单次反射）
- 将接收机对候选平面做镜像，求镜像点到卫星方向与平面交点。
- 检查交点在面片内且路径无遮挡。
- 计算额外路径长度 `ΔL`，得到等效伪距偏差。

### 4.3 强度与可观测性评分
- 基于：入射角、反射系数、传播距离、遮挡次数、天线方向图。
- 输出 `multipath score`，用于和 C/N0 或残差做相关性检验。

## 5. 与观测量耦合（建议先做可解释再做学习）

### 5.1 可解释基线模型
- 输入：`ΔL`, 入射角, 反射面类别, 卫星高度角, 方位角。
- 输出：伪距残差偏移、C/N0 衰减概率。
- 方法：分段线性/广义线性模型（GLM）。

### 5.2 数据驱动增强
- 在可解释特征上再加 XGBoost/LightGBM。
- 任务 A：判定 LOS vs NLOS。
- 任务 B：预测残差绝对值区间。

## 6. 实验设计（你后续论文/报告最需要）

### 6.1 切分策略
- 场景切分：街谷、开阔区、转角、玻璃幕墙区。
- 时间切分：不同日/时段。
- 卫星切分：按高度角分层（低仰角更关键）。

### 6.2 评价指标
- 分类：Precision / Recall / F1（NLOS 检测）。
- 回归：MAE / RMSE（残差预测）。
- 定位级：3D RMS, 95% CEP 改善量。

### 6.3 对照组
- 无 LiDAR 模型（仅 C/N0 阈值）。
- 仅 LOS 阻挡模型（不做反射路径）。
- 完整几何反射模型（你的主方法）。

## 7. 推荐的“先做什么”执行清单（按优先级）

### 第 1 周（打地基）
1. 导入 IPNL 香港点云，确认坐标系与单位。
2. 把 UBX 转为 RINEX / 可读观测表。
3. 跑通卫星位置计算和 ENU 转换。
4. 在 1 条短轨迹上实现 LOS 射线判定。

### 第 2 周（核心几何）
1. 完成平面分割 + 反射面集合。
2. 完成单次反射路径求解（镜像法）。
3. 产出每历元每卫星 `ΔL + score`。

### 第 3 周（验证闭环）
1. 对齐观测残差/CN0，做相关性图。
2. 训练基线分类器（LOS/NLOS）。
3. 输出第一版误差改善结果。

### 第 4 周（论文化）
1. 消融实验（无反射系数、无遮挡检查等）。
2. 鲁棒性实验（点云降采样、配准扰动）。
3. 整理图表与方法流程图。

## 8. 你现在可以立即补齐的仓库结构（建议）

```text
gnss_ws/
  data/
    raw/
      ubx/
      lidar_ipnl_hk/
    processed/
      rinex/
      pointcloud_tiles/
  src/
    io/
    coord/
    lidar/
    raytracing/
    modeling/
  notebooks/
  experiments/
  outputs/
  docs/
```

## 9. 风险点与规避
- **坐标错位**：会直接让反射建模失真 -> 必做基准点验证。
- **材质未知**：先做类别常数，不要一开始就上复杂电磁模型。
- **计算量过大**：先局部 tile + 候选面筛选，再全量。
- **验证闭环断裂**：必须把几何输出映射到真实观测残差，不然难发表。

## 10. 预期产出（建议你按这个清单交付）
1. 一份可复现实验配置（数据、参数、脚本）。
2. 一套可解释几何模型（LOS/NLOS + 单反射）。
3. 一份与传统方法对照的量化改进结果。
4. 一版论文草稿图表（方法图 + 结果表 + 典型案例图）。
