# IPNL 香港数据集后续研究执行清单

## 0. 目前仓库可见内容（先说结论）
- 我目前在 `gnss_ws` 仓库里只能看到一个 UBX 原始观测文件：
  - `2026-4-15_174245_serial-COM5(1).ubx`
- 没有看到你说的“这段时间和 Claude 的完整工作代码/文档/实验脚本”（例如 `src/`、`notebooks/`、`docs/`、`results/` 等目录目前都不存在）。

> 这意味着：你的后续研究是可以继续规划的，但需要先把“数据接口、代码骨架、实验记录模板”补齐，避免后续复现困难。

---

## 1. 研究目标重述（建议作为论文/项目固定描述）
**课题**：GNSS signal reflection modelling using LiDAR dense point clouds。

**核心问题**：利用高密度 LiDAR 点云重建城市反射体几何（墙面、玻璃、地面、屋顶等），解释并预测 GNSS 多路径（Multipath）和 NLOS（非视距）观测偏差。

**最终交付建议**：
1. 可复现的数据处理流水线（GNSS + LiDAR + 轨迹 + 地图坐标系统一）；
2. 反射路径几何求解器（镜面反射 + 可见性遮挡判断）；
3. 每颗卫星、每历元的 LoS / NLoS / Multipath 标签与风险评分；
4. 和观测残差（伪距、载波相位、CN0、Doppler）对齐后的统计验证；
5. 可用于导航滤波器的误差模型（加权或剔除策略）。

---

## 2. 你现在最该做的“具体清单”（按优先级）

## P0：数据资产盘点（1–2 天）
- [ ] 列清单：IPNL 香港数据集中每个子数据的格式、采样率、时间系统、坐标系；
- [ ] 确认 GNSS 原始观测是否含：伪距、载波、Doppler、CN0、卫星星历/导航电文；
- [ ] 确认 LiDAR 是原始点云还是语义化点云（是否已有建筑/地面分类）；
- [ ] 确认轨迹真值来源（INS/RTK/SLAM）和精度区间；
- [ ] 对每段数据生成统一 `metadata.yaml`（时间范围、传感器时钟偏差、坐标定义、缺失字段）。

## P1：统一时空基准（2–4 天）
- [ ] 统一时间：GPS Time / UTC / UNIX，明确 leap second 处理；
- [ ] 统一坐标：WGS84(LLA) ↔ ECEF ↔ ENU（以车辆轨迹或站点为参考原点）；
- [ ] 做“时间对齐报告”：GNSS 与 LiDAR 时间偏移估计（静态段 + 动态段双重验证）；
- [ ] 输出对齐后的主索引表：`epoch_id, t_gps, sat_id, rx_pose, lidar_frame_id`。

## P2：城市反射体建模（4–7 天）
- [ ] 点云去噪与降采样（保边界，减少运算）；
- [ ] 平面/曲面分割（RANSAC + region growing 或法向聚类）；
- [ ] 估计每个候选反射面的法向量、粗糙度、材质代理特征（玻璃/混凝土可用反射强度+语义先验）；
- [ ] 建立空间索引（KD-tree / voxel / BVH）支持快速射线求交；
- [ ] 生成“可反射体地图”并保存中间产物（可复用）。

## P3：GNSS 反射路径求解（5–10 天）
- [ ] 每历元根据星历算卫星位置，先做 LoS 可见性判定；
- [ ] 对每个候选反射面做镜像法（specular reflection）求单次反射路径；
- [ ] 检查几何可行性：入射角=反射角、路径无遮挡、反射点在面内；
- [ ] 计算路径差 `ΔL`、额外时延 `Δτ`、相位偏移 `Δφ`；
- [ ] 计算权重：由入射角、距离衰减、材质代理、天线方向图综合得到；
- [ ] 输出每个 `epoch-sat` 的候选反射集合与主反射路径。

## P4：观测域验证与标注（4–7 天）
- [ ] 计算无约束残差（伪距/载波）并与预测 `ΔL` 对齐；
- [ ] 依据几何+信号指标打标签：LoS / Multipath / NLoS；
- [ ] 验证指标：
  - 残差绝对值与预测路径差相关性；
  - CN0 降低与遮挡程度一致性；
  - 方位/高度角分组后的统计稳定性；
- [ ] 产出混淆矩阵与 ROC/PR 曲线（若做分类模型）。

## P5：用于定位解算的模型落地（4–8 天）
- [ ] 方案A：基于几何风险的观测加权（WLS/EKF 中调整测量噪声）；
- [ ] 方案B：高风险观测剔除（门限+连续性约束）；
- [ ] 方案C：显式偏差校正（把预测反射偏差从观测中扣除）；
- [ ] 对比 baseline：
  - 无建模；
  - 仅 CN0 门限；
  - 仅卫星高度角门限；
  - 你的 LiDAR 反射模型；
- [ ] 报告最终增益：水平/垂向 RMS、95%误差、可用卫星数、定位连续性。

---

## 3. 推荐实验设计（你可以直接照着做）

## E1：几何可解释性实验（先做）
**目的**：证明 LiDAR 几何能解释残差变化。
- 输入：同一路段多历元 GNSS + LiDAR；
- 输出：
  - 卫星-历元级别 `ΔL_pred` vs `residual_obs` 散点图；
  - 反射点可视化（地图+射线）；
- 判据：显著相关（分场景：街谷、开阔区、十字路口）。

## E2：标签质量实验
**目的**：验证 LoS/NLoS/Multipath 判别有效。
- 用规则法先打“银标准标签”；
- 若有人工可视审查，抽样核验 200–500 条；
- 评估：precision/recall/F1，按卫星高度角分桶。

## E3：定位收益实验（核心）
**目的**：展示工程价值。
- 同一轨迹做四组解算（baseline 到 LiDAR 模型）；
- 指标：RMS、95%、最大误差、失锁恢复时间；
- 输出：误差箱线图 + CDF + 典型街区轨迹图。

---

## 4. 风险点与规避策略
- **时间同步错误**：会直接毁掉相关性；先做时间偏移扫描（例如 ±2 s）。
- **坐标系混乱**：严格固定 ENU 原点和旋转定义；所有模块统一读同一个配置文件。
- **点云过密导致算力爆炸**：先分块+语义筛选（只保留潜在反射面）。
- **材质未知导致振幅建模不稳**：先做“几何主导 + 经验权重”，后续再加材质细化。
- **过拟合单场景**：至少拆分成 3 类路段（开阔/半遮挡/城市峡谷）做分层评估。

---

## 5. 建议的仓库结构（马上就能建）
```text
gnss_ws/
  data/
    raw/
    interim/
    processed/
    metadata/
  src/
    io/
    time_sync/
    coord/
    lidar_surface/
    raytrace/
    gnss_model/
    labeling/
    evaluation/
  configs/
    dataset_ipnl_hk.yaml
    experiment_e1.yaml
    experiment_e3.yaml
  notebooks/
  outputs/
    figures/
    tables/
  docs/
    research_log.md
    decisions.md
```

---

## 6. 两周推进版本（现实可执行）

## Week 1
- Day 1–2：完成 P0 + P1；
- Day 3–5：完成点云反射面提取最小版（P2）；
- Day 6–7：打通单历元/单卫星反射路径求解（P3 最小闭环）。

## Week 2
- Day 8–10：批量跑反射预测并与残差对齐（P4）；
- Day 11–12：完成 WLS 加权版定位改进（P5 方案A）；
- Day 13–14：产出 E1/E3 图表与小结。

---

## 7. 下一步你只要给我这 6 样东西，我就能继续细化到“可直接开工”的级别
1. IPNL 香港数据集目录树（`find data -maxdepth 3` 输出即可）；
2. 一段 5–10 分钟样例数据（GNSS + LiDAR + 轨迹）；
3. 你目前可用的解算器（RTKLIB / 自研 / 其他）；
4. 你希望优先优化的指标（RMS、95%、连续性、可用性）；
5. 你期望论文目标（方法型/工程型/两者兼顾）；
6. 计算资源约束（CPU/GPU、可接受运行时长）。

